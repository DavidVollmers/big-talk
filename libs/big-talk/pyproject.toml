[project]
name = "big-talk-ai"
version = "0.0.0"
description = "Biggest talk in town"
requires-python = ">=3.12"
dependencies = []

[project.optional-dependencies]
anthropic = ["anthropic>=0.79.0"]
openai = ["openai>=2.21.0", "tiktoken>=0.12.0"]
